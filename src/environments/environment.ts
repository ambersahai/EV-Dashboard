export const environment = {
  production: true,
  apiUrl: 'https://api.yourdomain.com/api',  // Replace with your production API URL
  googleAnalyticsKey: 'YOUR-PROD-GA-KEY',   // Replace with your production Google Analytics tracking key
  featureFlags: {
    enableNewFeature: true,  // Set to true for production if a new feature is enabled
  }
};
